# Secrets
.env

# Database WAL/SHM (transient) — but ledger.db IS committed (GitHub = cloud store for CCR)
data/ledger.db-wal
data/ledger.db-shm

# Local-only run results (CCR writes these; we pull them)
run_results/*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Logs
logs/*.log

# OS
.DS_Store
Thumbs.db

# Temp files
/tmp/
*.tmp

# Original zip
*.zip
check_state.py
