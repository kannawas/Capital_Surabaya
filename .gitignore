# Secrets
.env

# Database WAL/SHM (transient) — but ledger.db IS committed (GitHub = cloud store for CCR)
data/ledger.db-wal
data/ledger.db-shm

# run_results JSON snapshots ARE committed by CCR (so we can pull them)
# dashboard.json IS committed (the UI fetches it via GitHub Pages)

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Logs
logs/*.log

# OS
.DS_Store
Thumbs.db

# Temp files
/tmp/
*.tmp

# Original zip
*.zip
check_state.py
